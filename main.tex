% Documento principal de Anexos
\documentclass[12pt,twoside]{article}
% Idioma y codificación
\usepackage[spanish]{babel}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}

% Tipografía y espaciado (estilo similar a APA)
\usepackage{mathptmx}
\usepackage{setspace}
\doublespacing

% Márgenes APA: 1 pulgada (2.54 cm)
\usepackage[letterpaper,margin=1in]{geometry}

% Encabezados y formato de secciones
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{graphicx}
% Buscar imágenes en la carpeta images/ por conveniencia
\graphicspath{{../DocuProyectoFinal/images/}{images/}}
\usepackage{enumitem}
\usepackage{underscore}
% Configure hyperref to avoid duplicate destination names for pages
% `hypertexnames=false` prevents repeated `page.N` identifiers.
\usepackage[hidelinks,hypertexnames=false,pdfpagelabels=false]{hyperref}
% Ensure page numbering is not reset by any package or by calls to \pagenumbering
% (some titlepage/cover inputs might call \pagenumbering; preserve continuity).
\newcounter{savedpageforcontinuous}
\let\origpagenumbering\pagenumbering
\renewcommand{\pagenumbering}[1]{%
  \setcounter{savedpageforcontinuous}{\value{page}}%
  \origpagenumbering{#1}%
  \setcounter{page}{\value{savedpageforcontinuous}}%
}
\usepackage{bookmark}
\usepackage{placeins}
\usepackage{pdfpages}
\usepackage{float}  % Para usar [H] en tablas y figuras
\usepackage{array}  % Para mejor control de columnas
\usepackage{longtable}  % Para tablas que ocupan múltiples páginas

% Configuración para que longtable respete mejor los márgenes
\setlength{\LTpre}{0pt}
\setlength{\LTpost}{0pt}
\setlength{\LTleft}{0pt}
\setlength{\LTright}{\fill}

% Forzar salto de página antes de cada sección/subsección (cada capítulo en anexos)
\usepackage{etoolbox}
\preto\section{\clearpage}
\preto\subsection{\clearpage}

% Añadir soporte para un cuarto nivel de secciones
\setcounter{secnumdepth}{4}
\setcounter{tocdepth}{3}
% Hacer que las subsecciones actúen como numeración principal dentro de cada anexo
% y que los subsubsections muestren la numeración como <subsection>.<subsubsection>
\renewcommand{\thesubsection}{\arabic{subsection}}
\renewcommand{\thesubsubsection}{\thesubsection.\arabic{subsubsection}}
\makeatletter
\renewcommand\paragraph{\@startsection{paragraph}{4}{\z@}%
  {3.25ex \@plus 1ex \@minus .2ex}%
  {1.5ex \@plus .2ex}%
  {\normalfont\normalsize\bfseries}}
\makeatother
\newcommand{\subsubsubsection}[1]{\paragraph{#1}}

% Forzar justificación completa en todo el documento
\usepackage{ragged2e}

% Configuración de encabezado
\pagestyle{fancy}
\setlength{\headheight}{14.5pt}
\fancyhf{}
\fancyhead[L]{\textit{Anexos}}
\fancyhead[R]{\thepage}
\renewcommand{\headrulewidth}{0pt}

% Secciones estilo APA
\titleformat{\section}{\normalfont\bfseries\large}{\thesection.}{0.5em}{}
\titleformat{\subsection}{\normalfont\bfseries}{\thesubsection.}{0.5em}{}

% Asegurar que todo el documento quede justificado desde el inicio
\AtBeginDocument{\justifying}
\begin{document}

% Portada personalizada
\input{portada_anexos_partial_fix.tex}
\clearpage

% Índice
\tableofcontents
\clearpage

% Secciones principales de anexos
\input{anexos.tex}

\end{document}
